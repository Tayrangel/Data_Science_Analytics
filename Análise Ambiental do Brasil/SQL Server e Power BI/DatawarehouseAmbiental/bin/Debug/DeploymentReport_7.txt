** Destaques
     Tabelas que serão recriadas com os dados migrados
       [dbo].[dimTempo]
       [dbo].[fCobertura]
     Índices clusterizados que serão removidos
       Nenhum
     Índices clusterizados que serão criados
       Nenhum
     Possíveis problemas de dados
       Ignorando a coluna [dbo].[fCobertura].[id_dia]; poderá ocorrer perda de dados.

** Ações do usuário
     Remover
       [dbo].[FK_fCobertura_dimTempo] (Chave Estrangeira)
     Recriar tabela
       [dbo].[dimTempo] (Tabela)
       [dbo].[fCobertura] (Tabela)
     Criar
       [dbo].[FK_fCobertura_dimTempo] (Chave Estrangeira)

** Ações de suporte
     Remover
       [dbo].[FK_fCobertura_DimBioma] (Chave Estrangeira)
       [dbo].[FK_fCobertura_dimLocalizacao] (Chave Estrangeira)
       [dbo].[FK_fCobertura_dimClasse] (Chave Estrangeira)
     Criar
       [dbo].[FK_fCobertura_DimBioma] (Chave Estrangeira)
       [dbo].[FK_fCobertura_dimLocalizacao] (Chave Estrangeira)
       [dbo].[FK_fCobertura_dimClasse] (Chave Estrangeira)

A coluna ano na tabela [dbo].[dimTempo] deve ser alterada de NULL para NOT NULL. Se a tabela contiver dados, o script ALTER talvez não funcione. Para evitar o problema, você deve adicionar valores a essa coluna para todas as linhas, marcá-la para permitir valores NULL ou habilitar a geração de padrões inteligentes como uma opção de implantação.
Ignorando a coluna [dbo].[fCobertura].[id_dia]; poderá ocorrer perda de dados.
A coluna ano na tabela [dbo].[fCobertura] deve ser alterada de NULL para NOT NULL. Se a tabela contiver dados, o script ALTER talvez não funcione. Para evitar o problema, você deve adicionar valores a essa coluna para todas as linhas, marcá-la para permitir valores NULL ou habilitar a geração de padrões inteligentes como uma opção de implantação.

